# ── Hermes Platform 镜像构建忽略表 ──
# 目标：镜像只包含运行所需，不含本机密钥/会话数据/构建缓存。

# Git / CI 元数据
.git
.github
.gitignore

# Python 缓存
**/__pycache__
**/*.pyc
**/*.pyo
**/.pytest_cache

# 本机密钥（容器内用 k8s Secret 挂载，绝不烤进镜像）
.env
.jwt_secret
*.pem
*.key

# 会话数据（PVC 提供，绝不进镜像）
hermes_sessions/
/tmp/

# 日志
*.log
/tmp/broker.log

# 文档与计划（运行不需要）
docs/
CLAUDE.md
README.md

# service.sh 准备的构建产物（hermes-agent/ uv-python/）由 Dockerfile 显式 COPY，
# 不能在此排除（.dockerignore 是全局的，会连显式 COPY 一起挡掉）。
# Dockerfile 现在用显式 COPY 而非 COPY .，不会再把它们误拷进 /opt/hermes-platform。
