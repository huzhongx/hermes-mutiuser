# ──────────────────────────────────────────────────────────────────────────
# docker-compose 用环境变量示例
# 用法：cp .env.example .env，然后编辑 .env
# ⚠️ .env 不要提交到 git（已在 .gitignore）
# ──────────────────────────────────────────────────────────────────────────

# ── 必填：对外域名 ──
# broker 用它生成 ws_url/http_url（客户端看到的完整地址）
# 例: hermes.yourdomain.com
HERMES_NGINX_DOMAIN=hermes.example.com

# ── 可选：TCR 镜像命名空间（若用 B 镜像源）──
# TCR_NAMESPACE=your-tcr-namespace